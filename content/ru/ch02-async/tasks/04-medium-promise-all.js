// Реализуй свой promiseAll(promises)
// Работает как Promise.all: ждёт все, возвращает массив результатов
// Если хоть один реджектится — весь результат реджектится

export function promiseAll(promises) {
  // твой код здесь
}
