// Реализуй async-функцию parallelLimit(tasks, limit)
// tasks — массив функций, каждая возвращает промис
// Выполняй не более limit задач одновременно
// Верни массив результатов в исходном порядке

export async function parallelLimit(tasks, limit) {
  // твой код здесь
}
