/**
 * Задача: Проверка чётности числа
 *
 * Напишите функцию isEven, которая принимает число
 * и возвращает true если оно чётное, false если нечётное.
 *
 * Используйте оператор остатка от деления %.
 *
 * Примеры:
 * isEven(4)    // true
 * isEven(7)    // false
 * isEven(0)    // true  — 0 считается чётным
 * isEven(-2)   // true
 * isEven(-3)   // false
 */

/**
 * Проверяет чётность числа через оператор %
 * @param {number} n - Целое число
 * @returns {boolean}
 */
export function isEven(n) {
  // TODO: реализуйте функцию (одна строка!)
}
