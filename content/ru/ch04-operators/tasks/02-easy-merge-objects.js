/**
 * Задача: Слияние объектов через spread
 *
 * Напишите функцию mergeObjects, которая принимает произвольное
 * количество объектов через rest-параметры и возвращает их слияние.
 *
 * При конфликте ключей — последний объект побеждает.
 * Функция не должна мутировать входные объекты.
 *
 * Примеры:
 * mergeObjects({ a: 1 }, { b: 2 })          // { a: 1, b: 2 }
 * mergeObjects({ a: 1 }, { a: 2 })          // { a: 2 }
 * mergeObjects({ a: 1 }, { b: 2 }, { c: 3 }) // { a: 1, b: 2, c: 3 }
 * mergeObjects()                             // {}
 * mergeObjects({ a: 1 })                    // { a: 1 }
 */

/**
 * Сливает несколько объектов в один через spread
 * @param {...Object} objects - Объекты для слияния
 * @returns {Object} Новый объект с объединёнными свойствами
 */
export function mergeObjects(...objects) {
  // TODO: реализуйте функцию (одна строка с reduce или spread!)
}
