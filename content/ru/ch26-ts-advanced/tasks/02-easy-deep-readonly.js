/**
 * Задача: deepFreeze — рекурсивный Object.freeze
 *
 * Реализуйте функцию deepFreeze(obj), которая замораживает объект
 * и все вложенные объекты рекурсивно.
 *
 * После вызова deepFreeze нельзя изменять ни одно свойство
 * на любом уровне вложенности.
 *
 * @param {T} obj - объект для заморозки
 * @returns {Readonly<T>} замороженный объект
 */
export function deepFreeze(obj) {
  // TODO: заморозьте текущий объект через Object.freeze
  // и рекурсивно заморозьте все вложенные объекты
}
