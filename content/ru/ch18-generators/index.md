# Генераторы и итераторы

Генераторы — это функции, которые умеют «ставить себя на паузу» и возобновлять выполнение. Они реализуют протокол итерации и открывают целый класс паттернов: бесконечные последовательности, ленивые вычисления, конечные автоматы и асинхронные потоки данных.

import { Callout, CrossLink } from '@book/ui'

<Callout type="concept">
Генератор — это функция, объявленная с `function*`, которая возвращает объект-итератор. Каждый вызов `yield` «замораживает» выполнение и возвращает значение наружу. Вызов `.next()` размораживает его с того же места.
</Callout>

## Что изучим

- **[Протокол итерации](./01-iterator-protocol)** — `Symbol.iterator`, `for...of`, spread и деструктуризация
- **[Генераторы: синтаксис и поведение](./02-generators-syntax)** — `function*`, `yield`, передача значений обратно
- **[Практические применения](./03-practical-generators)** — бесконечные диапазоны, ленивый map/filter, конечные автоматы
- **[Async-генераторы](./04-async-generators)** — `async function*`, `for await...of`, постраничная загрузка

<CrossLink chapter="ch06-loops" title="Циклы и итерация" />
<CrossLink chapter="ch07-functions" title="Функции и замыкания" />
<CrossLink chapter="ch02-async" title="Асинхронность и промисы" />
