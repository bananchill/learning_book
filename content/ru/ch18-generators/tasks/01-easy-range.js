/**
 * Задача: Генератор диапазона
 *
 * Реализуй генератор range(start, end, step = 1), который
 * выдаёт числа от start до end (не включая end) с шагом step.
 *
 * Примеры:
 *   [...range(1, 5)]      → [1, 2, 3, 4]
 *   [...range(0, 10, 2)]  → [0, 2, 4, 6, 8]
 *   [...range(5, 1, -1)]  → [5, 4, 3, 2]
 *   [...range(0, 0)]      → []
 */

export function* range(start, end, step = 1) {
  // твой код здесь
}
