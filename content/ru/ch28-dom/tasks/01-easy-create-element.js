/**
 * Задача: createElement — фабрика DOM-элементов
 *
 * Реализуйте функцию createElement(tag, attrs, children):
 * - tag: строка с именем тега ('div', 'span', 'button' и т.д.)
 * - attrs: объект атрибутов { class: 'btn', type: 'button' } (опционально)
 * - children: строка или массив (строк/элементов) (опционально)
 *
 * @param {string} tag
 * @param {Record<string, string>} [attrs]
 * @param {string | Array<string | Element>} [children]
 * @returns {HTMLElement}
 */
export function createElement(tag, attrs = {}, children = []) {
  // TODO: реализуйте функцию
}
