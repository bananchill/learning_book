/**
 * Задача: renderList — эффективное обновление списка
 *
 * Реализуйте функцию renderList(container, items, renderItem):
 * - container: DOM элемент-контейнер
 * - items: массив данных
 * - renderItem: функция (item) => HTMLElement
 *
 * Функция должна:
 * 1. Очистить контейнер
 * 2. Создать DocumentFragment
 * 3. Для каждого item создать элемент через renderItem(item)
 * 4. Добавить все элементы во fragment
 * 5. Добавить fragment в container
 *
 * @param {Element} container
 * @param {Array} items
 * @param {(item: any) => Element} renderItem
 */
export function renderList(container, items, renderItem) {
  // TODO: реализуйте функцию
}
