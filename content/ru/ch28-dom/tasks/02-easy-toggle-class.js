/**
 * Задача: toggleClass — переключение CSS классов
 *
 * Реализуйте функцию toggleClass(el, className, force):
 * - el: DOM элемент
 * - className: строка с именем класса
 * - force: (опционально)
 *   - true → только добавить класс
 *   - false → только удалить класс
 *   - undefined → переключить (добавить если нет, удалить если есть)
 *
 * Возвращает: true если класс был добавлен, false если удалён
 *
 * @param {Element} el
 * @param {string} className
 * @param {boolean} [force]
 * @returns {boolean}
 */
export function toggleClass(el, className, force) {
  // TODO: реализуйте функцию
}
