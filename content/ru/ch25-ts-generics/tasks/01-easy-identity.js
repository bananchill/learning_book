/**
 * Задача: Generic функция identity
 *
 * Реализуйте функцию identity, которая принимает значение
 * любого типа и возвращает его без изменений.
 *
 * @param {T} value - значение любого типа
 * @returns {T} то же самое значение
 */
export function identity(value) {
  // TODO: реализуйте функцию
}
