/**
 * Задача: Generic класс Stack<T>
 *
 * Реализуйте класс Stack с методами:
 * - push(item) — добавляет элемент на вершину
 * - pop() — удаляет и возвращает верхний элемент (или undefined если пустой)
 * - peek() — возвращает верхний элемент без удаления (или undefined если пустой)
 * - size — геттер, возвращает количество элементов
 * - isEmpty() — возвращает true если стек пустой
 */
export class Stack {
  // TODO: реализуйте класс
}
