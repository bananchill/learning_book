/**
 * Задача: Типобезопасное обновление поля
 *
 * Реализуйте функцию updateField, которая:
 * 1. Принимает объект, ключ и новое значение
 * 2. Возвращает копию объекта с обновлённым полем
 * 3. TypeScript должен проверять, что значение соответствует типу поля
 *
 * Также реализуйте функцию updateFields, которая принимает
 * объект и Partial<T> с обновлениями, и возвращает обновлённую копию.
 */

// TODO: добавьте параметры типов и правильные аннотации
export function updateField(obj: any, key: any, value: any): any {
  // TODO: верните копию obj с обновлённым полем
}

// TODO: добавьте параметры типов
export function updateFields(obj: any, updates: any): any {
  // TODO: верните копию obj с применёнными обновлениями
}
