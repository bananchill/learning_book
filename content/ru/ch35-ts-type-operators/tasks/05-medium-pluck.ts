/**
 * Задача: Функция pluck
 *
 * Реализуйте функцию pluck, которая:
 * 1. Принимает массив объектов и имя ключа
 * 2. Возвращает массив значений этого ключа из каждого объекта
 * 3. Тип возвращаемого значения должен быть T[K][]
 *
 * Также реализуйте функцию pluckMany, которая принимает
 * массив ключей и возвращает массив объектов с только этими ключами.
 */

// TODO: добавьте параметры типов и правильные аннотации
export function pluck(arr: any[], key: any): any[] {
  // TODO: извлеките значения по ключу из каждого элемента
}

// TODO: добавьте параметры типов
export function pluckMany(arr: any[], keys: any[]): any[] {
  // TODO: для каждого элемента оставьте только указанные ключи
}
