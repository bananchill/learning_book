/**
 * Задача: @readonly декоратор для свойств класса
 *
 * Реализуйте функцию readonly(target, propertyKey),
 * которую можно использовать как декоратор свойства.
 * После применения свойство нельзя изменить.
 *
 * @param {object} target - прототип класса
 * @param {string} propertyKey - имя свойства
 */
export function readonly(target, propertyKey) {
  // TODO: используйте Object.defineProperty чтобы сделать свойство нередактируемым
}
