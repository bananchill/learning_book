/**
 * Задача: Безопасное сложение
 *
 * Напишите функцию safeAdd, которая принимает два аргумента
 * и возвращает их сумму, если оба являются числами (не NaN).
 * В противном случае возвращает NaN.
 *
 * Требования:
 * - Оба аргумента должны быть числами (typeof === 'number')
 * - Ни один аргумент не должен быть NaN
 * - Строковые числа ('5') не должны приниматься
 *
 * Примеры:
 * safeAdd(2, 3)       // 5
 * safeAdd(1.5, 2.5)   // 4
 * safeAdd(5, '3')     // NaN — строка, не число
 * safeAdd('2', '3')   // NaN
 * safeAdd(NaN, 1)     // NaN
 * safeAdd(null, 1)    // NaN
 * safeAdd(undefined, 1) // NaN
 */

/**
 * Складывает два числа, если оба являются валидными числами
 * @param {*} a - Первое значение
 * @param {*} b - Второе значение
 * @returns {number} Сумма или NaN
 */
export function safeAdd(a, b) {
  // TODO: реализуйте функцию
}
