// Создай createRateLimiter(fn, maxCalls, intervalMs)
// Ограничивает вызовы fn: не более maxCalls за intervalMs
// Лишние вызовы возвращают undefined

export function createRateLimiter(fn, maxCalls, intervalMs) {
  // твой код здесь
}
