/**
 * Задача 1: Светофор (easy)
 *
 * Создайте строковый enum TrafficLight с тремя сигналами: Red, Yellow, Green.
 * Значения: "RED", "YELLOW", "GREEN".
 *
 * Реализуйте функцию getAction, которая принимает сигнал светофора
 * и возвращает строку с действием:
 * - Red -> "Стоп"
 * - Yellow -> "Внимание"
 * - Green -> "Можно ехать"
 */

// Ваш код здесь:

export enum TrafficLight {
  // Задайте члены enum
}

export function getAction(signal: TrafficLight): string {
  // Реализуйте функцию
  throw new Error("Не реализовано");
}
