// Создай функцию atomicIncrement(sharedView, index)
// sharedView — Int32Array на SharedArrayBuffer
// index — индекс элемента
// Атомарно увеличивает значение на 1
// Возвращает НОВОЕ значение (после инкремента)

export function atomicIncrement(sharedView, index) {
  // твой код здесь
}
