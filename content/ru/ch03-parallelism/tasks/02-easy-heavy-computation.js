// Создай функцию computeInWorker(fn, arg)
// fn — чистая функция (без замыканий), arg — аргумент
// Выполни fn(arg) в inline Web Worker
// Верни промис с результатом

export function computeInWorker(fn, arg) {
  // твой код здесь
}
