// Создай функцию createInlineWorker(code)
// Принимает строку с JS-кодом
// Возвращает Web Worker, созданный из Blob (inline worker)

export function createInlineWorker(code) {
  // твой код здесь
}
