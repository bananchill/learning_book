// Создай функцию parallelSum(numbers, workerCount)
// Разбивает массив на workerCount частей
// Суммирует каждую часть в отдельном inline-воркере
// Возвращает промис с общей суммой
// Воркеры должны работать параллельно (Promise.all)

export function parallelSum(numbers, workerCount) {
  // твой код здесь
}
