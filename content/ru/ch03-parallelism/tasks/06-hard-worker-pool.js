// Реализуй класс WorkerPool
//
// constructor(workerCode, size)
//   workerCode — строка с JS-кодом воркера (inline worker)
//   size — количество воркеров в пуле
//
// exec(data) → Promise
//   Отправляет задачу свободному воркеру
//   Если все заняты — ставит в очередь
//   Возвращает промис с результатом
//
// destroy()
//   Завершает все воркеры

export class WorkerPool {
  constructor(workerCode, size) {
    // твой код здесь
  }

  exec(data) {
    // твой код здесь
  }

  destroy() {
    // твой код здесь
  }
}
