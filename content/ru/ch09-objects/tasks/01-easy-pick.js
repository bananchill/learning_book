/**
 * Задача: Функция pick
 *
 * Реализуйте функцию pick(obj, keys), которая возвращает
 * новый объект, содержащий только указанные ключи из obj.
 *
 * Ключи, которых нет в obj, в результат не включаются.
 *
 * Примеры:
 * pick({ a: 1, b: 2, c: 3 }, ['a', 'c'])  // { a: 1, c: 3 }
 * pick({ a: 1, b: 2 }, ['b'])              // { b: 2 }
 * pick({ a: 1 }, ['x'])                    // {}
 * pick({}, ['a', 'b'])                     // {}
 */

/**
 * Возвращает подмножество объекта по списку ключей
 * @param {Object} obj - Исходный объект
 * @param {string[]} keys - Ключи для включения в результат
 * @returns {Object} Новый объект с выбранными ключами
 */
export function pick(obj, keys) {
  // TODO: реализуйте через Object.fromEntries и filter/map
}
