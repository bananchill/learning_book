/**
 * Задача: Глубокое клонирование объекта
 *
 * Реализуйте функцию deepClone(obj), которая создаёт полную
 * независимую копию объекта без использования structuredClone.
 *
 * Требования:
 * - Поддержать примитивы (числа, строки, булевы, null, undefined)
 * - Поддержать вложенные объекты
 * - Поддержать массивы (включая вложенные)
 * - НЕ нужно поддерживать: Date, Map, Set, функции, циклические ссылки
 *
 * Примеры:
 * const a = { x: { y: 1 }, arr: [1, 2, 3] };
 * const b = deepClone(a);
 * b.x.y = 99;
 * a.x.y; // 1 — не изменилось
 *
 * const arr = [[1, 2], [3, 4]];
 * const cloned = deepClone(arr);
 * cloned[0][0] = 99;
 * arr[0][0]; // 1 — не изменилось
 */

/**
 * Рекурсивно клонирует объект или массив
 * @param {*} obj - Значение для клонирования
 * @returns {*} Глубокая копия
 */
export function deepClone(obj) {
  // TODO: реализуйте рекурсивное клонирование
  // 1. Если примитив (не объект или null) — вернуть как есть
  // 2. Если массив — скопировать каждый элемент рекурсивно
  // 3. Если объект — скопировать каждое свойство рекурсивно
}
