/**
 * Задача: Функция omit
 *
 * Реализуйте функцию omit(obj, keys), которая возвращает
 * новый объект без указанных ключей.
 *
 * Примеры:
 * omit({ a: 1, b: 2, c: 3 }, ['b'])        // { a: 1, c: 3 }
 * omit({ a: 1, b: 2, c: 3 }, ['a', 'c'])   // { b: 2 }
 * omit({ a: 1 }, ['x'])                     // { a: 1 } (ключа нет — не меняется)
 * omit({ a: 1, b: 2 }, [])                  // { a: 1, b: 2 }
 */

/**
 * Возвращает объект без указанных ключей
 * @param {Object} obj - Исходный объект
 * @param {string[]} keys - Ключи для исключения
 * @returns {Object} Новый объект без исключённых ключей
 */
export function omit(obj, keys) {
  // TODO: реализуйте через Object.fromEntries и filter
  // Подсказка: используйте Set для быстрой проверки ключей
}
