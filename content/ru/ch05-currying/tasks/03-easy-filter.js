// Реализуй каррированную функцию filter(predicate)(array)
// Она принимает предикат и возвращает функцию, которая фильтрует массив
//
// Пример:
//   const isEven = x => x % 2 === 0
//   filter(isEven)([1, 2, 3, 4]) // [2, 4]
//
//   const greaterThan = n => x => x > n
//   filter(greaterThan(2))([1, 2, 3, 4]) // [3, 4]

export function filter(predicate) {
  // твой код здесь
}
