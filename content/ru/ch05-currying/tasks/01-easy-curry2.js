// Реализуй функцию curry2, которая принимает функцию с двумя аргументами
// и возвращает каррированную версию: curry2(fn)(a)(b) === fn(a, b)
//
// Пример:
//   const add = (a, b) => a + b
//   const curriedAdd = curry2(add)
//   curriedAdd(2)(3) // 5

export function curry2(fn) {
  // твой код здесь
}
