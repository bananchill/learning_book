// Реализуй функцию analyzeCaptures(outerVars, innerUsedVars)
// outerVars — массив имён переменных внешней функции
// innerUsedVars — массив имён переменных, используемых во вложенной функции
//
// Верни объект { context: [...], stack: [...] }
// context — переменные, попадающие в Context (куча) — используются в замыкании
// stack — переменные, остающиеся на стеке — НЕ используются в замыкании

export function analyzeCaptures(outerVars, innerUsedVars) {
  // твой код здесь
}
