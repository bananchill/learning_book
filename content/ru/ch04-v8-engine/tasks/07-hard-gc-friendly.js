// Реализуй функцию createObjectPool(factory, maxSize)
// factory — функция, создающая новый объект
// maxSize — максимальный размер пула
//
// Возвращает объект с методами:
// - acquire() — берёт объект из пула (или создаёт новый через factory)
// - release(obj) — возвращает объект в пул (если пул не полон)
// - size() — текущий размер пула (количество свободных объектов)
// - clear() — очищает пул

export function createObjectPool(factory, maxSize) {
  // твой код здесь
}
