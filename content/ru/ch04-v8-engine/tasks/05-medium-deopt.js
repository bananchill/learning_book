// Реализуй функцию findDeoptRisk(fn, testCases)
// fn — функция для анализа
// testCases — массив массивов аргументов: [[arg1, arg2], [arg1, arg2], ...]
// Верни true, если типы аргументов меняются между вызовами (риск деоптимизации)
// Верни false, если типы стабильны

export function findDeoptRisk(fn, testCases) {
  // твой код здесь
}
