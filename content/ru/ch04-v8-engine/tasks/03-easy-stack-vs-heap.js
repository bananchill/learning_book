// Реализуй функцию classifyStorage(value)
// Возвращает 'stack' для примитивов (number, boolean, string, undefined, null, bigint, symbol)
// Возвращает 'heap' для объектов, массивов, функций

export function classifyStorage(value) {
  // твой код здесь
}
