// Реализуй функцию sameHiddenClass(objA, objB)
// Возвращает true, если два объекта имеют одинаковый hidden class
// (одинаковые свойства в одинаковом порядке)

export function sameHiddenClass(objA, objB) {
  // твой код здесь
}
