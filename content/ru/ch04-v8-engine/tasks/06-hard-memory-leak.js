// Реализуй функцию createLeakDetector()
// Возвращает объект с методами:
// - track(key, obj) — отслеживает объект через WeakRef
// - check() — возвращает массив ключей, чьи объекты ещё живы
// - getLeaked() — возвращает массив ключей, чьи объекты были собраны GC

export function createLeakDetector() {
  // твой код здесь
}
