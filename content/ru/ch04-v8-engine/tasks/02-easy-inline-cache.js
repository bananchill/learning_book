// Реализуй функцию classifyIC(objects)
// Принимает массив объектов, возвращает состояние inline cache:
// 'monomorphic' — все объекты одного shape (один набор ключей в одном порядке)
// 'polymorphic' — 2-4 разных shape
// 'megamorphic' — 5+ разных shape

export function classifyIC(objects) {
  // твой код здесь
}
