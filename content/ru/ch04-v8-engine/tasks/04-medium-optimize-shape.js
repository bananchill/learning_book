// Реализуй функцию createUsers(rawData)
// rawData — массив объектов с полями name, age, email (в произвольном порядке,
// возможны дополнительные поля)
// Верни новый массив объектов, где каждый имеет ровно три свойства:
// name, age, email — в этом порядке (для одинакового hidden class)

export function createUsers(rawData) {
  // твой код здесь
}
