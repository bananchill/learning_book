// Задача: создать объект через Object.create с методом в прототипе
// createAnimal(name, sound) должна вернуть объект где:
//   speak() — выводит в console.log: "${name} говорит: ${sound}"
//   speak должен находиться в прототипе (не в самом объекте)

export function createAnimal(name, sound) {
  // твой код здесь
}
