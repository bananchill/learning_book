/**
 * Задача: Повтор вызова функции
 *
 * Напишите функцию repeat, которая принимает функцию fn и число n,
 * вызывает fn ровно n раз и возвращает массив результатов каждого вызова.
 *
 * fn вызывается с индексом текущей итерации (0, 1, 2, ...) как аргументом.
 *
 * Примеры:
 * repeat(i => i * 2, 4)       // [0, 2, 4, 6]
 * repeat(() => 'привет', 3)   // ['привет', 'привет', 'привет']
 * repeat(i => i ** 2, 5)      // [0, 1, 4, 9, 16]
 * repeat(fn, 0)               // [] — ноль вызовов
 */

/**
 * Вызывает fn n раз, передавая индекс итерации (0-based)
 * @param {Function} fn - Функция для повтора, принимает индекс
 * @param {number} n - Количество повторений
 * @returns {Array} Массив результатов
 */
export function repeat(fn, n) {
  // TODO: реализуйте
}
