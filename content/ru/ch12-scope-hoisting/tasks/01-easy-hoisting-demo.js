// Задача: продемонстрировать поведение hoisting
// Функция hoistingDemo() должна вернуть массив из 4 элементов:
// [0] typeof переменной var до её объявления
// [1] результат вызова функции до её объявления (строка из функции)
// [2] значение var после объявления
// [3] строка 'TDZ Error' — поймай ReferenceError при обращении к let до объявления

export function hoistingDemo() {
  // твой код здесь
  // Подсказка: var-переменная и функция должны быть объявлены НИЖЕ их использования
}
