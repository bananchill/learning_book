// Задача: реализовать паттерн модуля через IIFE
// createModule() должна вернуть объект с публичным API:
//   add(item)    — добавляет элемент в приватный массив
//   remove(item) — удаляет первое вхождение элемента
//   getAll()     — возвращает КОПИЮ массива (не оригинал!)
//   count()      — возвращает количество элементов
// Внутренний массив items должен быть недоступен снаружи

export function createModule() {
  // твой код здесь
  // Используй IIFE для создания приватного скоупа
}
