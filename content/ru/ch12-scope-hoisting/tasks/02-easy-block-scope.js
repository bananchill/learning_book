// Задача: исправить классическую проблему замыкания в цикле
// Функция blockScope(n) должна вернуть массив из n функций
// При вызове каждая функция должна вернуть свой индекс: fns[0]() === 0, fns[1]() === 1, ...

export function blockScope(n) {
  // твой код здесь
  // Используй let в цикле, чтобы каждая итерация имела свою переменную
}
