// Задача: реализовать безопасное деление через паттерн Result
// safeDivide(a, b) возвращает:
//   { ok: true, value: a / b }   — при успехе
//   { ok: false, error: Error }  — при b === 0

export function safeDivide(a, b) {
  // твой код здесь
}
