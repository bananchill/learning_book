// Задача: реализовать аналог Function.prototype.bind
// myBind(fn, ctx, ...preArgs) возвращает новую функцию которая:
//   — вызывает fn с this === ctx
//   — поддерживает частичное применение: preArgs объединяются с args при вызове

export function myBind(fn, ctx, ...preArgs) {
  // твой код здесь
}
