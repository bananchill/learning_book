// Задача: привязать все методы объекта к нему самому
// bindAll(obj) — привязывает все собственные методы obj к obj через bind
// Изменяет объект на месте, возвращает его

export function bindAll(obj) {
  // твой код здесь
}
