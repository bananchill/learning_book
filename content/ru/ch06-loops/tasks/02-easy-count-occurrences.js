/**
 * Задача: Подсчёт вхождений
 *
 * Напишите функцию countOccurrences, которая принимает массив и значение,
 * и возвращает количество раз, которое значение встречается в массиве.
 *
 * Сравнение должно быть строгим (===).
 *
 * Примеры:
 * countOccurrences([1, 2, 3, 2, 1], 2)  // 2
 * countOccurrences([1, 2, 3, 2, 1], 1)  // 2
 * countOccurrences([1, 2, 3], 5)         // 0
 * countOccurrences([], 1)                // 0
 * countOccurrences(['a', 'b', 'a'], 'a') // 2
 * countOccurrences([1, '1', 1], 1)       // 2 (строгое сравнение: '1' не считается)
 */

/**
 * Считает количество вхождений значения в массиве
 * @param {Array} arr - Массив для поиска
 * @param {*} value - Искомое значение
 * @returns {number} Количество вхождений
 */
export function countOccurrences(arr, value) {
  // TODO: реализуйте через цикл
}
