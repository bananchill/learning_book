/**
 * Задача: Сумма от 1 до n
 *
 * Напишите функцию sumTo, которая принимает число n
 * и возвращает сумму всех целых чисел от 1 до n включительно.
 *
 * Реализуйте через цикл (не через формулу n*(n+1)/2).
 *
 * Примеры:
 * sumTo(1)  // 1
 * sumTo(5)  // 15   (1+2+3+4+5)
 * sumTo(10) // 55   (1+2+...+10)
 * sumTo(0)  // 0
 * sumTo(-1) // 0    (нет чисел для суммирования)
 */

/**
 * Вычисляет сумму 1+2+...+n через цикл
 * @param {number} n - Верхняя граница (включительно)
 * @returns {number} Сумма
 */
export function sumTo(n) {
  // TODO: реализуйте через цикл for или while
}
