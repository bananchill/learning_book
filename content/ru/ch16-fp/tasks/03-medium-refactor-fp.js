// Задача: рефакторинг обработки заказов в функциональный стиль
//
// processOrders(orders) должна:
//   1. Оставить только активные заказы (status === 'active')
//   2. Преобразовать в формат { id, total, currency }
//   3. Отсортировать по total убыванию
//   4. Вернуть первые 5 заказов
//
// Использовать pipe, чистые функции, без мутаций

// Формат входных данных:
// orders = [{ id, status, items: [{price, qty}], currency }]

export function processOrders(orders) {
  // твой код здесь — используй pipe и чистые функции
}
