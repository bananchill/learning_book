// Задача: реализовать функцию pipe
// pipe(...fns) возвращает новую функцию
// которая применяет fns слева направо:
// pipe(f, g, h)(x) === h(g(f(x)))

export function pipe(...fns) {
  // твой код здесь
}
