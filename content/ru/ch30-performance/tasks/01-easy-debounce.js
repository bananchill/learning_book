/**
 * Задача: debounce — откладывает вызов функции
 *
 * Реализуйте функцию debounce(fn, delay):
 * - Откладывает вызов fn на delay мс
 * - Если вызов происходит снова до истечения задержки, таймер сбрасывается
 * - Возвращает обёрнутую функцию
 *
 * Пример использования: debounce для search input —
 * не отправляем запрос при каждом нажатии, только когда пользователь остановился
 *
 * @param {Function} fn
 * @param {number} delay
 * @returns {Function}
 */
export function debounce(fn, delay) {
  // TODO: реализуйте debounce
}
