/**
 * Задача: throttle — ограничивает частоту вызовов
 *
 * Реализуйте функцию throttle(fn, interval):
 * - fn вызывается не чаще одного раза за interval мс
 * - Первый вызов выполняется немедленно
 * - Повторные вызовы во время паузы игнорируются
 *
 * Пример использования: throttle для scroll handler —
 * не обрабатываем каждый пиксель скролла, только раз в 100 мс
 *
 * @param {Function} fn
 * @param {number} interval
 * @returns {Function}
 */
export function throttle(fn, interval) {
  // TODO: реализуйте throttle
}
