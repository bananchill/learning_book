# Proxy и Reflect

`Proxy` позволяет «обернуть» объект и перехватывать почти любые операции с ним: чтение, запись, удаление свойств, вызов, проверку `in` и многое другое. `Reflect` — его напарник, предоставляющий стандартные реализации этих операций.

import { Callout, CrossLink } from '@book/ui'

<Callout type="concept">
`new Proxy(target, handler)` — создаёт прокси для `target`. Каждый метод в `handler` — это «ловушка» (trap), перехватывающая конкретную операцию. Если ловушки нет — операция проходит к оригинальному объекту напрямую.
</Callout>

## Что изучим

- **[Proxy: ловушки и handler](./01-proxy-basics)** — get, set, has, deleteProperty и другие трапы
- **[Reflect API](./02-reflect)** — стандартные операции, правильное делегирование
- **[Практические паттерны](./03-practical-proxy)** — валидация, логирование, кэширование, иммутабельность
- **[Реактивность: Vue 3 под капотом](./04-reactivity)** — как Proxy лежит в основе `reactive()`

<CrossLink chapter="ch09-objects" title="Объекты и дескрипторы свойств" />
<CrossLink chapter="ch13-prototypes" title="Прототипы и наследование" />
