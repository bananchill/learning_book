/**
 * Задача: Уникальные элементы массива
 *
 * Напишите функцию unique, которая принимает массив
 * и возвращает новый массив без дубликатов.
 *
 * При дубликатах сохраняется первое вхождение.
 * Порядок остальных элементов сохраняется.
 * Сравнение строгое (===).
 *
 * Примеры:
 * unique([1, 2, 3, 2, 1])          // [1, 2, 3]
 * unique(['a', 'b', 'a', 'c'])     // ['a', 'b', 'c']
 * unique([1, '1', 1])              // [1, '1'] — 1 и '1' разные
 * unique([])                        // []
 * unique([1, 2, 3])                // [1, 2, 3] — нет дубликатов
 */

/**
 * Возвращает массив с уникальными значениями
 * @param {Array} arr - Исходный массив
 * @returns {Array} Массив без дубликатов
 */
export function unique(arr) {
  // TODO: реализуйте через Set или filter с indexOf
}
