/**
 * Задача: Pipeline функций
 *
 * Напишите функцию pipeline, которая принимает произвольное количество функций
 * и возвращает новую функцию. Эта функция принимает начальное значение
 * и применяет все функции последовательно (слева направо).
 *
 * Похоже на compose, но в обратном порядке: pipeline(f, g, h)(x) = h(g(f(x)))
 *
 * Примеры:
 * const process = pipeline(
 *   x => x + 1,    // прибавить 1
 *   x => x * 2,    // умножить на 2
 *   x => x - 3     // вычесть 3
 * );
 * process(5); // ((5 + 1) * 2) - 3 = 9
 *
 * const stringify = pipeline(
 *   Math.abs,      // абсолютное значение
 *   Math.round,    // округлить
 *   String         // в строку
 * );
 * stringify(-3.7); // '4'
 *
 * pipeline()(5); // 5 — без функций возвращает значение как есть
 */

/**
 * Создаёт pipeline из функций (применяется слева направо)
 * @param {...Function} fns - Функции для последовательного применения
 * @returns {Function} Функция, применяющая все fns к значению
 */
export function pipeline(...fns) {
  // TODO: используйте reduce для последовательного применения функций
}
