// Задание: напиши функцию format, которая принимает string | number | boolean
// и возвращает строку по правилам:
// - string → перевести в верхний регистр
// - number → округлить до 2 знаков после запятой и добавить " руб."
// - boolean → вернуть "да" или "нет"

export function format(value: string | number | boolean): string {
  // твой код здесь
  return "";
}

// Бонус: напиши функцию formatAll, которая принимает массив (string | number | boolean)[]
// и возвращает массив отформатированных строк
export function formatAll(values: (string | number | boolean)[]): string[] {
  // твой код здесь
  return [];
}
