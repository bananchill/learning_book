/**
 * Задача: Шаблонизатор
 *
 * Реализуйте функцию parseTemplate(template, data), которая заменяет
 * плейсхолдеры вида {{key}} в строке template на соответствующие
 * значения из объекта data.
 *
 * Требования:
 * - Плейсхолдеры вида {{key}} (двойные фигурные скобки)
 * - Если ключ не найден в data — оставить плейсхолдер как есть
 * - Поддержать вложенные ключи через точку: {{user.name}}
 *
 * Примеры:
 * parseTemplate('Привет, {{name}}!', { name: 'Иван' })
 * // 'Привет, Иван!'
 *
 * parseTemplate('{{a}} + {{b}} = {{c}}', { a: 1, b: 2 })
 * // '1 + 2 = {{c}}'  — c не найден, остаётся как есть
 *
 * parseTemplate('Привет, {{user.name}}!', { user: { name: 'Мария' } })
 * // 'Привет, Мария!'
 */

/**
 * Заменяет плейсхолдеры {{key}} в шаблоне на значения из data
 * @param {string} template - Строка-шаблон с плейсхолдерами
 * @param {Object} data - Объект с данными для подстановки
 * @returns {string} Строка с подставленными значениями
 */
export function parseTemplate(template, data) {
  // TODO: реализуйте функцию
  // Подсказка: используйте str.replace() с regex и функцией-обработчиком
  // Для вложенных ключей: разбейте по '.' и обходите объект
}
